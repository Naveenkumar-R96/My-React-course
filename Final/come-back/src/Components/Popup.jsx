import React from 'react'

const Popup = ({copied}) => {
  return (
    
      <section>
        {copied && (
            <div>copied to clipboard</div>
        )}
        <p>iruku</p>
      </section>
      
        
       

  )
}

export default Popup
